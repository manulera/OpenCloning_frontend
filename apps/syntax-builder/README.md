# Syntax Builder

## How to run dev server

From the root of the repository, run:

```bash
yarn workspace syntax-builder dev
```

## How to build

From the root of the repository, run:

```bash
yarn workspace syntax-builder build
```
